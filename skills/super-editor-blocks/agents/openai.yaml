interface:
  display_name: "Super Editor Blocks"
  short_description: "Manage blocks and identity-safe JSON replacement"
  default_prompt: "Use $super-editor-blocks to manage blocks or safely replace exported block JSON while preserving every identity anchor."
